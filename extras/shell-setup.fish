# Da Vinci Console — fish shell setup
# Add to ~/.config/fish/config.fish

# Optional: pin repo search dirs (colon-separated, tilde supported).
# Without this, the picker auto-scans ~/ for git repos.
set -gx SESH_REPO_DIRS "$HOME/dev:$HOME/projects"
